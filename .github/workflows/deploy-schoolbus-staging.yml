# Deprecated — use deploy-staging.yml (unified PG + School Bus on rg-saasstack-staging).
name: Deploy School Bus Staging (deprecated)

on:
  workflow_dispatch:
    inputs:
      note:
        description: This workflow is retired; run "Deploy Staging" instead
        required: false
        type: string

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::deploy-schoolbus-staging.yml is deprecated."
          echo "Use .github/workflows/deploy-staging.yml (unified staging on main/staging)."
          echo "Do not deploy to rg-saasstack-sb-staging; use rg-saasstack-staging."
          exit 1
